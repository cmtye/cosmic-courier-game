using UnityEngine;
using UnityEngine.Events;
using Level_Scripts.Interaction;

//public class InteractionEvent : UnityEvent<PlayerController, InteractionHandler>
//{
//}
 
public class GameEventListener : MonoBehaviour
{
    //public GameEvent Event;
    //public UnityEvent Response;
 
    //private void OnEnable() => Event.RegisterListener(this);
 
    //private void OnDisable() => Event.UnregisterListener(this);
 
    //public void OnEventRaised(PlayerController player, InteractionHandler handler) => Response.Invoke(player, handler);
}
